// The component used to structure the Contacts section

function MiniContacts(props) {
    return <div>
        <svg></svg>
        <p>{props.contactsParagraph}</p>
    </div>
}

export default MiniContacts